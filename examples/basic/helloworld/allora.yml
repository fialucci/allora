# Allora HelloWorld example - single channel configuration
version: 1
channels:
  - id: input_channel
  - id: output_channel
    kind: queue
service-activators:
  - id: svc.hello
    ref-name: hello_world
    from: input_channel
    to: output_channel
