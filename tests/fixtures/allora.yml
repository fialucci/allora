# Top-level Allora configuration fixture (v1)
version: 1
channels:
  - kind: direct
    id: inbound.orders
  - kind: direct
    id: processed.orders
  - kind: direct
    id: error.deadletter
filters:
  - id: filt.orders
    from: inbound.orders
    to: processed.orders
    when: body.contains("KEEP") && exists(header("Trace-Id"))
